# AFyC Conciliador API
API mínima en Node + Express conectada a Supabase.

## Endpoints
- `/` → Verifica que el servicio esté activo
- `/health` → Comprueba conexión con Supabase
- `/test-db` → Muestra hasta 3 usuarios de la tabla `Usuarios`
